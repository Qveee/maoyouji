# Repository Guidelines

## 项目结构与模块组织

《喵游记》目前处于设计文档与 UI 原型阶段，尚无游戏引擎工程。`docs/游戏规则设计.md` 是唯一权威设计源文档；`docs/清单/` 存放七份独立清单，冲突不得互相回写，只能在各自的“跨清单差异处理”章节记录。`prototype/` 为主原型、界面布局样例、背包/宠物栏风格参考页及 `sprites/` 战斗精灵。`assets/` 存放原始素材与授权信息。仓库根目录 `*.png` 是原版界面参考截图，不是可交付产物。

## 构建、测试与本地开发命令

- `python -m http.server 8000`：在 `prototype/` 目录启动本地静态服务，访问 `http://localhost:8000/猫游记界面UI.html`。
- 也可直接用浏览器打开任一 `prototype/*.html`。
- `git diff --check`：提交前检查空白错误。

本项目无构建步骤、包管理器或自动化测试。

## 编码风格与命名约定

所有文档、界面文案、注释与提交信息使用简体中文；文件保持 UTF-8，HTML 声明 `lang="zh-CN"`。原型必须继续采用单文件 HTML + CSS + 原生 JavaScript，保持零框架、零外部依赖。HTML 使用两空格缩进；CSS/JS 遵循现有紧凑风格。继续使用 `①②③` 分区注释、语义化小写连字符 ID/class，并在主原型中复用现有 CSS 变量与 2008 页游面板风格。新风格探索页命名如 `背包样式参考.html`、`宠物栏示例1-*.html`。

## 测试指南

没有正式测试框架时，至少完成手动回归：打开受影响页面，确认 1400×832 主框架、目标面板、交互状态与控制台无错误；样式参考页需并排核对所有变体。修改战斗、背包或宠物栏时，覆盖悬停、菜单、稀有度边框和资源加载路径。

## 提交与 Pull Request 规范

历史提交以中文作用域开头，例如 `prototype: ...`、`docs: ...`、`assets: ...`，组合改动可用 `docs+prototype: ...`。摘要应说明新增或调整的对象与关键结果。PR 需包含改动原因、影响范围、验证方式；UI 改动附截图或录屏；新增素材附来源与授权；涉及设计口径时说明与权威文档/清单的一致性。

## 安全与素材授权

新增或移动素材必须同步更新 `assets/来源与授权.md`，严禁引入授权不明资源。禁止提交凭据、CDN 依赖或自动联网脚本。中文路径在 shell 命令中始终加引号。
