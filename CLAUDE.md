# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## 项目概况

《喵游记》——复刻经典 2007 页游《猫游记》的多人在线回合制宠物养成 RPG。当前处于**设计文档 + UI 原型**阶段，尚无游戏引擎工程、无构建/测试体系。所有内容均为简体中文，与用户交流、写文档、写提交信息都用中文。

## 常用操作

- 原型是单文件 HTML，无依赖、无构建：直接用浏览器打开 `prototype/*.html` 预览即可；如需本地服务可在 `prototype/` 下跑 `python -m http.server`。
- 中文文件名/路径在 shell 中需加引号；Windows 控制台默认 GBK，跑输出中文的 Python 脚本前先 `export PYTHONIOENCODING=utf-8`。
- 若 git 报 `detected dubious ownership`，加 `-c safe.directory=D:/maoyouji` 参数执行。

## 文档体系（核心架构）

设计知识的组织方式比任何单个文件都重要：

```
docs/游戏规则设计.md          ← 源文档（唯一权威设计文档，当前 v1.5）
docs/清单/*.md               ← 7 份衍生清单（装备/道具/技能/怪物/宠物/任务/商城）
assets/来源与授权.md          ← 素材来源与授权台账
```

**源文档版本规则**：`游戏规则设计.md` 通过文首追加「vX.X 修订」行演进，**新修订覆盖旧设定**。注意 v1.5 已移除饱食度、心情值、AI 猫仙人三个系统（v1.1 加入的），不要重新引入；地图也已从节点图改为自由网格探索 + 镜头视野。

**清单纪律（严格遵守）**：
- 每份清单开头声明「只新增，不回写任何既有文件」——清单之间或清单与源文档冲突时，**不修改对方**，在各自的「跨清单差异处理」章节记录双来源/裁决（例：怪物清单 §1.6）。
- 条目标注：`提取`（来自源文档/其他清单）vs `◆补全`（按既有数值体系新设计）。补全必须沿用已有公式体系，不私设数值。
- 稳定 ID 体系：怪物 `MON_N/E/B_xxx`（普通/精英/BOSS）、装备 `WEP_/ARM_/ACC_` 前缀、道具 `FOOD_/MAT_/GRO_/QST_` 等；跨清单引用一律带 ID + 名称。
- 关键数值口径（写任何新内容前先对齐）：六维属性 VIT/STR/AGI/INT/SPR/LUK 及派生公式（宠物清单 §0）、品质梯度 灰<绿<蓝<紫<橙（装备清单 §1.1）、12 装备栏位、怪物数值模板公式（怪物清单 §1.1）、8 张地图各 3 普通 + 1 精英 + 1 BOSS。

## 原型体系（prototype/）

全部为**纯 HTML + CSS + 原生 JS 单文件，零外部依赖、零框架**——这一约定必须保持。

- `猫游记界面UI.html`：主原型，1400×832 游戏框架，三栏六区布局（对应源文档 §13 界面布局设计）。代码按 `① ② ③…` 分区注释组织（顶部状态栏/地图+聊天/怪物列表/掉落记录/私聊/底部操作栏），后加的面板（任务列表、背包、公会、好友、队伍）各自成段。主原型内的面板一律沿用 2008 页游风格：宋体、浅蓝渐变标题栏、CSS 变量（`--panel-top/--panel-bot/--ink-navy/--link-blue` 等）。
- `背包样式参考.html`、`宠物栏样式参考.html`：风格探索页，一个文件里并排多套皮肤变体，供选型对比。
- `宠物栏示例1/2-*.html`：DNF 式纸娃娃布局示例（12 装备格环绕居中角色立绘，悬停 tooltip + 点击替换/卸下菜单，灰绿蓝紫橙稀有度边框）。
- 新面板工作流：先做独立样式参考页供用户选型，选定后再合入主原型并遵循主原型风格约定。
- 仓库根目录的 `*.png` 是原版《猫游记》UI 截图，作为复刻参考素材，不是项目产出物。

## 素材（assets/）

全部来自公开免费渠道（opengameart、Kenney 等），**授权各异（CC0 / CC-BY-SA / 免费需署名 / 见页面）**，任何对外使用前先查 `assets/来源与授权.md`。战斗用怪物精灵已从 CC0 的 50+ Monsters Pack 复制 7 只到 `prototype/sprites/`（文件名 `monster-*.png`）。例外：`assets/原版参考/` 存放原作《猫游记》的参考图（宠物立绘等），版权归原权利人，仅限复刻参考、禁止对外使用。
